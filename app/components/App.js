var React = require('react');

class App extends React.Component {
  render() {
    return(
      <div>
        <h1>React Trivia</h1>
        <p>Please select a category:</p>
      </div>
    );
  }
}

module.exports = App;
